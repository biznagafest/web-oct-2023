---
import type { TeamMember } from "../data/data.type";
interface Props {
  member: TeamMember;
  kind: "organizer" | "staff";
  index: number;
}

const { member, index, kind } = Astro.props;
---

<a href={`/${kind}/${index}`} class="w-full max-w-[300px] h-fit block">
  <span class="flex flex-col align-middle justify-center">
    <img
      loading="lazy"
      class="object-cover h-full"
      src={member.picture}
      alt={`Picture of ${member.name}`}
    />
    <h4
      class="w-full text-2xl font-semibold text-center text-dark-background dark:text-light-background p-2"
    >
      {member.name}
    </h4>
  </span>
</a>
